name: Check Trailing Whitespace

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
    - run: if grep -rIn "[[:blank:]]$"; then; else exit 1; fi
    
